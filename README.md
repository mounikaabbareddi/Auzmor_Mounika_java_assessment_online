# Auzmor_Mounika_java_assessment
 Build a Http Server using java  Netty server that acceptsa JSON object and saves it into a Kafka queue.Setup netty/Kafka on the dev machine in order to do that.
 1.Build a Http Server
 2.Receives query parameters from URL.
 3.Convert the query parameters into Protobuff.
 4.Push the Protobuf object into  Kafka queue.